extends ../common/layout

block layout-content
  h1 Edit Project

  form(method="POST" action="/admin/projects/edit/submit")

    input(type="hidden" name="id" value=project._id)

    div
      label Title
      input(type="text" name="title" value=project.title)

    div
      label Description
      input(type="text" name="description" value=project.description)

    div
      label Tech Stack
      input(type="text" name="techStack" value=project.techStack.join(", "))

    div
      label Year
      input(type="number" name="year" value=project.year)

    div
      label Live Link
      input(type="text" name="liveLink" value=project.liveLink)

    button(type="submit") Update Project